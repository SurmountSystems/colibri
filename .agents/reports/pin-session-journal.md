# Pin: session-death journal correlation

Extended project `AGENTS.md` section **Native crash logs (test runs)** on 2026-08-13.
Chat still does not receive dumps automatically. First artifacts are `native.log` and, when the windowing session died, `journalctl` around those timestamps.
The host journalctl-safari ban is for keyring and supply-chain fishing. It does not apply to a dead compositor or an operator ask for the journal.
`native.log` is not a full session story after compositor death. Correlate UTC clocks. Say so if the journal has no match.
No Sentry or crash reporter was added. Existing item 4 still forbids both.
No product code was changed.
